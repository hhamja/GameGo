import 'package:mannergamer/utilites/index.dart';

class SettingAlram extends StatelessWidget {
  const SettingAlram({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
